---
phase: 02-core-mod-loop
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - src/components/ModList.tsx
  - src/components/ModCard.tsx
autonomous: false
requirements: [IMPORT-01, IMPORT-02, IMPORT-03, IMPORT-04, IMPORT-05, IMPORT-06, TOGGLE-01, TOGGLE-02, TOGGLE-03, TOGGLE-05, TOGGLE-07, CONFLICT-01, CONFLICT-02, CONFLICT-03]

must_haves:
  truths:
    - "User can import a mod from a .zip file and see it in the mod list"
    - "User can toggle a mod on/off with one click, state persists across restart"
    - "PAK/ucas/utoc file triples appear grouped in a single mod"
    - "Sub-mod option folders are detected at import and toggleable independently"
    - "Enabling a conflicting mod shows warning with mod names and overlapping files"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint. Test the complete mod import, toggle, conflict, and delete flows with a real zip file.

Purpose: Ensure all the backend services and frontend UI work together as a cohesive experience before marking Phase 2 complete.
Output: Verified working mod loop or list of issues to fix.
</objective>

<execution_context>
@C:/Users/eric/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eric/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-mod-loop/02-CONTEXT.md
@.planning/phases/02-core-mod-loop/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any issues</name>
  <files>src/components/ModList.tsx, src/components/ModCard.tsx</files>
  <action>
    1. Run `cargo test -p modtoggler` -- fix any Rust test failures
    2. Run `npx tsc --noEmit` -- fix any TypeScript type errors
    3. Run `npx vitest run` -- fix any frontend test failures (if tests exist)
    4. Run `cargo tauri build --debug` or `cargo tauri dev` to verify the app launches
    5. Review all components for any obvious issues:
       - ModCard toggle switch properly greys out during mutation
       - ConflictDialog correctly groups conflicts by mod name
       - ImportDialog pre-fills name correctly
       - SubModOptions disabled when parent is off
    6. Fix any compilation or runtime issues found
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p modtoggler && cd .. && npx tsc --noEmit</automated>
  </verify>
  <done>All Rust and TypeScript tests pass. App compiles and launches without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete mod loop end-to-end</name>
  <files>src/components/ModList.tsx, src/components/ModCard.tsx, src/components/ImportDialog.tsx, src/components/ConflictDialog.tsx, src/components/SubModOptions.tsx</files>
  <action>
    Present the running application to the user for manual verification of the complete mod lifecycle:
    import from zip, toggle on/off, sub-mod options, conflict detection, and mod deletion.
    Launch the app with `cargo tauri dev` if not already running.
  </action>
  <verify>User confirms the mod loop works correctly by testing all flows listed in how-to-verify</verify>
  <done>User has approved the complete mod import, toggle, conflict, and delete experience.</done>
  <what-built>Complete mod import, toggle, conflict detection, and delete flow. Users can now:
    1. Import mods from .zip files (via Import button or drag-and-drop)
    2. See imported mods in a card-based list with toggle switches
    3. Toggle mods on/off (files move between staging and game directory)
    4. Expand mod cards to see file manifest and sub-mod options
    5. Toggle sub-mod options independently
    6. Receive conflict warnings when enabling a mod that overlaps with another
    7. Delete mods permanently from the expanded card view
  </what-built>
  <how-to-verify>
    1. Start the app with `cargo tauri dev`
    2. Select an existing game (or add one)
    3. Click "Import" button in the header -- pick a .zip file containing mod files
    4. Verify: import dialog shows with pre-filled name from filename
    5. Confirm import -- verify mod appears in the list as a card
    6. Click the mod card to expand it -- verify file manifest shows
    7. If the mod has Option_ folders, verify sub-mod options appear with individual toggles
    8. Click the toggle switch on the mod card -- verify it enables (files move to game dir)
    9. Toggle off -- verify files return to staging
    10. Import a second mod with overlapping files -- enable both and verify conflict warning dialog
    11. Test "Enable Anyway" in conflict dialog -- verify last-enabled wins
    12. Expand a mod, click Delete -- verify mod is removed
    13. Close and reopen the app -- verify toggle states persist
    14. (Optional) Drag a .zip file onto the mod list area -- verify import dialog opens
  </how-to-verify>
  <resume-signal>Type "approved" if the mod loop works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All automated tests pass
- Manual verification of the complete mod lifecycle
- Conflict detection correctly identifies overlapping files
- Sub-mod toggles work independently of parent
- State persists across app restarts
</verification>

<success_criteria>
- All Phase 2 success criteria from ROADMAP.md are met:
  1. User can import a mod from a .zip file and see it in the mod list with its file manifest
  2. User can toggle a mod on/off with one click, with state persisting across app restarts
  3. PAK/ucas/utoc file triples are auto-grouped into a single logical mod at import
  4. Sub-mod option folders are detected at import and can be toggled independently
  5. Enabling a mod that conflicts with an already-enabled mod shows a conflict warning naming both mods and the overlapping files
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-mod-loop/02-05-SUMMARY.md`
</output>
